# MySQL connection for Step 1 (DB init). Do NOT commit real credentials.
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=secret
DB_NAME=luckydraw

# Optional: server port (default 3000). Railway sets PORT automatically.
# PORT=3000

# Optional: CORS. Allow frontend origins (comma-separated for multiple).
# http://localhost:5173 is always allowed.
# FRONTEND_ORIGIN=https://your-app.vercel.app
# CORS_ORIGIN=https://your-app.vercel.app,https://your-app.railway.app
